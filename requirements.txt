# GARF — Google Ads query runner (read-only data pulls)
garf-executors==1.5.0
garf-google-ads==1.1.7

# Google Ads Python client — required for write operations (bid-budget-apply, creative-copy-apply)
google-ads==31.0.0
google-auth-oauthlib==1.4.0

# YAML — required for bid/budget plan files
pyyaml==6.0.3
