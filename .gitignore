# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
venv/
.venv/
env/

# Environment variables / secrets
.env

# --- Bob Frm Mktg ---

# Local account registry and per-account configs (local state, not for sharing)
.bob/profile.json
.bob/accounts.json
.bob/accounts/

# Raw API outputs
garf/outputs/raw/

# Processed data
data/processed/

# Wiki analyses (generated per session, per account — not for sharing)
wiki/

# Operational logs (backlog.md is tracked — it's a project artifact)
logs/pull-log.jsonl
logs/session-signals.jsonl

# Validation runtime outputs
validation/manual/*
!validation/manual/.gitkeep
validation/reports/*
!validation/reports/.gitkeep

# google-ads mutation and log output dirs
google-ads/logs/*
google-ads/mutations/*

# AI desktop app local config dirs
.antigravitycli/
.claude/settings.local.json

# Generated by setup.sh — local launcher with hardcoded venv path
/bob
