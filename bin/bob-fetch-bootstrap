#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [ -x "$ROOT/bob" ]; then
  exec "$ROOT/bob" bootstrap "$@"
else
  exec python3 "$ROOT/lib/datapull.py" bootstrap "$@"
fi
