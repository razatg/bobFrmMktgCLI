#!/usr/bin/env bash
# Compare MTD or full-month performance across two calendar months.
# Usage: bob-compare-months [--month N] [--vs M] [--year YYYY] [--full]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/../lib/datapull.py" compare-months "$@"
